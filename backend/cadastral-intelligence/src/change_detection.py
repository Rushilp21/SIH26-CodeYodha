"""Historical change detection. Owner: Developer 3. Stub only — do not claim live ML."""


def detect_changes(parcel_versions: list[dict]) -> list[dict]:
    """TODO(dev3): compare parcel_versions geometries in PROCESSING_CRS."""
    if len(parcel_versions) < 2:
        return []
    return []
