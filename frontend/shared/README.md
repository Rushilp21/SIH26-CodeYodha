# Shared frontend (Developer 6 OWNER)

## Purpose
Dumb UI primitives and TypeScript types aligned with `shared-schemas`. No business logic.

## Owner
Developer 6. Web-GIS (Dev 5) may **import** these components but must not rewrite them in `web-gis` copies if a shared piece exists.

## Do not
Put cadastral scoring, CRS conversion, or API orchestration here.
