# Web-GIS (Developer 5 ONLY)

## Purpose
GIS expert / surveyor review UI: map, queue, parcel detail, confidence, explanation, verify.

## Owner
Developer 5. Consume FastAPI only. Never talk to PostGIS.

## Run

```
cd frontend/web-gis
npm install
npm run dev
```

http://localhost:5173

Set `VITE_API_BASE_URL=http://localhost:8000` (default).

## Tests
Placeholder under `tests/`.
